/**
 * AirportLayer — renders major international airports as markers.
 * Status: placeholder — renders nothing until data is wired.
 * Data: src/layers/infrastructure/data/airports.sample.json
 */
import type { LayerProps } from '../_core/types'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function AirportLayer(_props: LayerProps) {
  // TODO: Load airports.sample.json, render Marker per airport
  // Icon: plane symbol, size by strategicImportance
  // Click: show airport detail tooltip (name, IATA, passengers, cargo, notes)
  return null
}
